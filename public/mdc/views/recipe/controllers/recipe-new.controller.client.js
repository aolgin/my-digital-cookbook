(function () {
    angular
        .module("MyDigitalCookbook")
        .controller("RecipeNewController", RecipeNewController);

    function RecipeNewController(RecipeService) {
        var vm = this;

        function init() {

        }
        init();
    }
})();